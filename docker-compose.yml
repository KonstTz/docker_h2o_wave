version: "1.0"
services:
  web:
    build: .
    ports:
      - "10101:10101"
    volumes:
      - ./data:/var/data
    links:
      - 3.10.0a3-slim-buster
  debian:
    image: 3.10.0a3-slim-buster
volumes:
  logvolume01: {}
